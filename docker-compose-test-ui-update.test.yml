services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.55.1-noble
    working_dir: /app
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ipc: host
    command: sh -c "npm ci && npx playwright test ./tests/ui.spec.ts --update-snapshots"
volumes:
  node_modules:
